{
  "private": true,
  "name": "clip-api",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "format": "prettier --write .",
    "lint": "tsc --noEmit && prettier --check ."
  },
  "dependencies": {
    "hono": "2.5.7"
  },
  "devDependencies": {
    "@types/workers": "*"
  }
}
