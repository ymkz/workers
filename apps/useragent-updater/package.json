{
  "private": true,
  "name": "useragent-updater",
  "scripts": {
    "dev": "wrangler dev src/index.ts --local --test-scheduled",
    "format": "rome format . --write",
    "lint": "rome check ."
  },
  "dependencies": {
    "hono": "2.5.0"
  }
}
